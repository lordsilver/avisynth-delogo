# Third-party Notices

The generated toolchain archive redistributes unmodified upstream binaries and scripts. Exact projects, versions, source URLs, release assets, licenses, and SHA-256 values are recorded in `toolchain.lock.json` and `sources.yaml` inside the archive.

The bundle includes GPL, Unlicense, and mixed-license components. Each component remains governed by its upstream license. Source projects and corresponding binary source assets are listed in the manifests; no component is relicensed by this repository.

Keep this notice, `toolchain.lock.json`, and `sources.yaml` with any redistributed copy of the binary archive. The matching `avisynth-delogo-sources-<version>.zip` release asset contains the exact upstream inputs used to build the binary archive, including the preserved FrameSel and RT_Stats author archives recovered through the AviSynth Wiki Internet Archive links.
